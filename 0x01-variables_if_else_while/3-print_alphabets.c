#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char a;
	char b;

	for (a = 'a'; a <= 'z'; c++)
		putchar(a);
	for (b = 'A'; b <= 'Z'; d++)
		putchar(b);
	putchar('\n');
	return (0);
}
