#include <stdio.h>

/**
 * main - Entry point
 * author: Josiah
 * Return: Always 0 (Success)
 */
int main(void)
{
	int b;

	for (b = 48; b < 58; b++)
		putchar(b);
	putchar('\n');
	return (0);
}
