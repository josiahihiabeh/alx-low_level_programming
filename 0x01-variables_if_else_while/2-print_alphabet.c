#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char josi;

	for (josi = 'a'; josi <= 'z'; josi++)
	{
		putchar(josi);
	}
	putchar('\n');
	return (0);
}
